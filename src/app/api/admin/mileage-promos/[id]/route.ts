import { NextResponse } from "next/server";import { auth } from "@/lib/auth/config";import { prisma } from "@/lib/db/prisma";
async function allowed(){return (await auth())?.user?.role==="ADMIN";}
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){if(!await allowed())return NextResponse.json({error:"Acesso negado."},{status:403});const {id}=await params;const body=await request.json() as {status?:"ACTIVE"|"INACTIVE"|"EXPIRED";bonusPercent?:number;validUntil?:string};const promo=await prisma.mileageTransferPromo.update({where:{id},data:{status:body.status,bonusPercent:body.bonusPercent,validUntil:body.validUntil?new Date(body.validUntil):undefined}});return NextResponse.json({promo});}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){if(!await allowed())return NextResponse.json({error:"Acesso negado."},{status:403});const {id}=await params;await prisma.mileageTransferPromo.delete({where:{id}});return NextResponse.json({ok:true});}
